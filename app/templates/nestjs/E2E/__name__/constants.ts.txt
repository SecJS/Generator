const dbPayload = {
  // Payload necessary to create <%= namePascal %> in Database
}

const storePayload = {
  // Payload necessary to create <%= namePascal %> in using POST
}

const updatePayload = {
  // Payload necessary to create <%= namePascal %> in using PUT
}

export { dbPayload, storePayload, updatePayload }
